from itertools import product

def base_conv(n, b):
    

def gen(n):
    s = str(n)
    for tup in


mem = {}
def f(n, B):
    if n < B:
        return 0
    if (n, B) in mem:
        return mem[(n, B)]
    else:
